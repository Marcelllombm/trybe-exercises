// criando a variavel nº
let number = 5;
let asteristic = "*";
let inputLine = "";

//criando o loop para percorrer a variavel n para criar a linha
    for(let lineIndex = 0; lineIndex < number; lineIndex +=1){
inputLine = inputLine + asteristic;


  }
// criando o loop para percorrer a variavel n para criar a quantidade de linha com variavel n
  for (let lineIndex = 0; lineIndex < number; lineIndex +=1  ){
    console.log(inputLine);
  }
